# =============================================================================
# Pages CMS 配置文件 —— isletopia-website
# 仓库：https://github.com/tobbylowrie/isletopia-website
# 框架：VitePress 2.0-alpha，srcDir 默认为项目根目录
# 文档：https://pagescms.org/docs
# =============================================================================

# -----------------------------------------------------------------------------
# 媒体（图片）上传目录
# VitePress 把项目根的 public/ 作为静态资源根，上传到 public/images/ 的文件
# 在站点中通过 /images/<filename> 访问。
# -----------------------------------------------------------------------------
media:
  - name: images
    label: 图片资源
    input: public/images
    output: /images
    extensions: [jpg, jpeg, png, webp, gif, svg]

# -----------------------------------------------------------------------------
# 内容集合
# -----------------------------------------------------------------------------
content:

  # ===========================================================================
  # 一、Feed 类文章（5 类，共用同一套 frontmatter 模板）
  # 这些目录的文章会被 NewsFeed.vue 聚合到 /update 动态页
  # ===========================================================================

  # 1. 新闻
  - name: news
    label: 新闻
    type: collection
    path: news
    filename: "{primary}.md"
    format: yaml-frontmatter
    view:
      fields: [title, date, hide]
      primary: title
      sort: date
      default: { sort: date, order: desc }
    fields:
      - { name: title, label: 标题, type: string, required: true }
      - { name: date, label: 发布日期, type: date, required: true }
      - { name: description, label: 摘要描述, type: text }
      - name: tags
        label: 标签
        type: string
        list: true
      - name: author
        label: 作者
        type: string
        list: true
        default: [梦幻之屿管理组]
      - { name: category, label: 分类, type: string, default: news, hidden: true }
      - { name: layout, label: 布局, type: string, default: doc, hidden: true }
      - { name: sidebar, label: 显示侧边栏, type: boolean, default: false, hidden: true }
      - { name: hide, label: 在动态页隐藏, type: boolean, default: false }
      - { name: aliases, label: 别名, type: string, list: true, hidden: true }
      - { name: body, label: 正文, type: rich-text }

  # 2. 博客
  - name: blogs
    label: 博客
    type: collection
    path: blogs
    filename: "{primary}.md"
    format: yaml-frontmatter
    view:
      fields: [title, date, hide]
      primary: title
      sort: date
      default: { sort: date, order: desc }
    fields:
      - { name: title, label: 标题, type: string, required: true }
      - { name: date, label: 发布日期, type: date, required: true }
      - { name: description, label: 摘要描述, type: text }
      - name: tags
        label: 标签
        type: string
        list: true
      - name: author
        label: 作者
        type: string
        list: true
        default: [梦幻之屿管理组]
      - { name: category, label: 分类, type: string, default: blogs, hidden: true }
      - { name: layout, label: 布局, type: string, default: doc, hidden: true }
      - { name: sidebar, label: 显示侧边栏, type: boolean, default: false, hidden: true }
      - { name: hide, label: 在动态页隐藏, type: boolean, default: false }
      - { name: aliases, label: 别名, type: string, list: true, hidden: true }
      - { name: body, label: 正文, type: rich-text }

  # 3. 活动公告
  - name: events
    label: 活动公告
    type: collection
    path: events
    filename: "{primary}.md"
    format: yaml-frontmatter
    view:
      fields: [title, date, hide]
      primary: title
      sort: date
      default: { sort: date, order: desc }
    fields:
      - { name: title, label: 标题, type: string, required: true }
      - { name: date, label: 发布日期, type: date, required: true }
      - { name: description, label: 摘要描述, type: text }
      - name: tags
        label: 标签
        type: string
        list: true
      - name: author
        label: 作者
        type: string
        list: true
        default: [梦幻之屿管理组]
      - { name: category, label: 分类, type: string, default: events, hidden: true }
      - { name: layout, label: 布局, type: string, default: doc, hidden: true }
      - { name: sidebar, label: 显示侧边栏, type: boolean, default: false, hidden: true }
      - { name: hide, label: 在动态页隐藏, type: boolean, default: false }
      - { name: aliases, label: 别名, type: string, list: true, hidden: true }
      - { name: body, label: 正文, type: rich-text }

  # 4. 更新日志（按日期命名文件）
  - name: changelog
    label: 更新日志
    type: collection
    path: changelog
    filename:
      template: "{year}-{month}-{day}.md"
      field: create
    format: yaml-frontmatter
    view:
      fields: [title, date]
      primary: title
      sort: date
      default: { sort: date, order: desc }
    fields:
      - { name: title, label: 标题, type: string, required: true }
      - { name: date, label: 发布日期, type: date, required: true }
      - { name: description, label: 摘要描述, type: text }
      - name: tags
        label: 标签
        type: string
        list: true
      - name: author
        label: 作者
        type: string
        list: true
        default: [梦幻之屿管理组]
      - { name: category, label: 分类, type: string, default: changelog, hidden: true }
      - { name: layout, label: 布局, type: string, default: doc, hidden: true }
      - { name: sidebar, label: 显示侧边栏, type: boolean, default: false, hidden: true }
      - { name: hide, label: 在动态页隐藏, type: boolean, default: false }
      - { name: aliases, label: 别名, type: string, list: true, hidden: true }
      - { name: body, label: 正文, type: rich-text }

  # 5. 公告
  - name: notices
    label: 公告
    type: collection
    path: notices
    filename: "{primary}.md"
    format: yaml-frontmatter
    view:
      fields: [title, date, hide]
      primary: title
      sort: date
      default: { sort: date, order: desc }
    fields:
      - { name: title, label: 标题, type: string, required: true }
      - { name: date, label: 发布日期, type: date, required: true }
      - { name: description, label: 摘要描述, type: text }
      - name: tags
        label: 标签
        type: string
        list: true
      - name: author
        label: 作者
        type: string
        list: true
        default: [梦幻之屿管理组]
      - { name: category, label: 分类, type: string, default: notices, hidden: true }
      - { name: layout, label: 布局, type: string, default: doc, hidden: true }
      - { name: sidebar, label: 显示侧边栏, type: boolean, default: false, hidden: true }
      - { name: hide, label: 在动态页隐藏, type: boolean, default: false }
      - { name: aliases, label: 别名, type: string, list: true, hidden: true }
      - { name: body, label: 正文, type: rich-text }

  # ===========================================================================
  # 二、静态文档集合
  # ===========================================================================

  # 6. 游玩指南（含 beginner/ 子目录）
  - name: guide
    label: 游玩指南
    type: collection
    path: guide
    subfolders: true
    filename: "{primary}.md"
    format: yaml-frontmatter
    view:
      fields: [title]
      primary: title
    fields:
      - { name: title, label: 标题, type: string }
      - { name: description, label: 页面描述, type: text }
      - { name: sidebar, label: 显示侧边栏, type: boolean, default: false }
      - { name: body, label: 正文, type: rich-text }

  # 7. 法律文档
  - name: legal
    label: 法律文档
    type: collection
    path: legal
    filename: "{primary}.md"
    format: yaml-frontmatter
    view:
      fields: [title]
      primary: title
    fields:
      - { name: title, label: 标题, type: string }
      - { name: description, label: 页面描述, type: text }
      - { name: sidebar, label: 显示侧边栏, type: boolean, default: false }
      - { name: body, label: 正文, type: rich-text }

  # 8. 媒体资源页（gallery / photos / replays / saves / works）
  - name: media-pages
    label: 媒体资源页
    type: collection
    path: media
    filename: "{primary}.md"
    format: yaml-frontmatter
    view:
      fields: [title]
      primary: title
    fields:
      - { name: title, label: 标题, type: string }
      - { name: description, label: 页面描述, type: text }
      - { name: sidebar, label: 显示侧边栏, type: boolean, default: false }
      - { name: body, label: 正文, type: rich-text }

  # ===========================================================================
  # 三、单文件
  # ===========================================================================

  # 9. 首页（根目录 index.md）
  # - name: home
  #   label: 首页
  #   type: file
  #   path: index.md
  #   format: yaml-frontmatter
  #   fields:
  #     - { name: layout, label: 布局, type: string, default: page, hidden: true }
  #     - { name: sidebar, label: 显示侧边栏, type: boolean, default: false, hidden: true }
  #     - { name: isHome, label: 是否为首页, type: boolean, default: true, hidden: true }
  #     - { name: body, label: 正文, type: rich-text }

  # 10. 动态聚合页（根目录 update.md）
  # - name: update-page
  #   label: 动态聚合页
  #   type: file
  #   path: update.md
  #   format: yaml-frontmatter
  #   fields:
  #     - { name: title, label: 标题, type: string }
  #     - { name: description, label: 页面描述, type: text }
  #     - { name: sidebar, label: 显示侧边栏, type: boolean, default: false, hidden: true }
  #     - { name: body, label: 正文, type: rich-text }

  # 11. 关于页面
  - name: about
    label: 关于页面
    type: file
    path: about.md
    format: yaml-frontmatter
    fields:
      - { name: title, label: 标题, type: string }
      - { name: description, label: 页面描述, type: text }
      - { name: sidebar, label: 显示侧边栏, type: boolean, default: false }
      - { name: body, label: 正文, type: rich-text }

  # 12. 加入服务器
  - name: join-us
    label: 加入服务器
    type: file
    path: join-us.md
    format: yaml-frontmatter
    fields:
      - { name: title, label: 标题, type: string }
      - { name: description, label: 页面描述, type: text }
      - { name: sidebar, label: 显示侧边栏, type: boolean, default: false }
      - { name: body, label: 正文, type: rich-text }

# -----------------------------------------------------------------------------
# 全局设置
# -----------------------------------------------------------------------------
settings:
  # merge: true —— 编辑时保留 schema 中未声明的 frontmatter 字段
  # 这对避免覆盖手动添加的 frontmatter 字段非常重要
  content:
    merge: true
  # commit: 用编辑者的 GitHub 身份提交，便于追溯
  commit:
    identity: user
    templates:
      create: "feat(cms): 新建 {path}"
      update: "update(cms): 更新 {path}"
      delete: "chore(cms): 删除 {path}"
      rename: "chore(cms): 重命名 {oldPath} -> {newPath}"
